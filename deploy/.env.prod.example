# Emergency direct-IP mode
# URL will be: http://SERVER_IP:8080
PUBLIC_PORT=8080
PORT=8080
RUST_LOG=messenger_server=info

# Future HTTPS mode with deploy/docker-compose.caddy.yml
# Set this only after DNS A record points to your server IP.
# DOMAIN=chat.example.com
